{
  "private": true,
  "scripts": {
    "dev": "npm run development",
    "development": "mix",
    "watch": "mix watch",
    "watch-poll": "mix watch -- --watch-options-poll=1000",
    "prod": "npm run production",
    "production": "mix --production",
    "workbench:install": "composer run workbench:install",
    "workbench:build": "composer run workbench:build",
    "workbench:serve": "composer run workbench:serve",
    "test": "composer run test"
  },
  "devDependencies": {
    "laravel-nova-devtool": "file:vendor/laravel/nova-devtool",
    "lodash": "^4.17.21"
  },
  "dependencies": {
    "codemirror": "^5.65.19"
  }
}
